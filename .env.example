# ============================================
# UptimeKit Environment Configuration
# ============================================
# Copy this file to .env and update values
# All apps and packages load from this single .env file

# ============================================
# Application Settings
# ============================================
APP_URL=http://localhost:3000
APP_SECRET=change_me_to_a_secure_secret # openssl rand -hex 32
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/uptimekit


# ============================================
# Time-series Backend
# ============================================
# Backend used to store monitor events/changes. One of: "clickhouse" | "timescale"
# - clickhouse: high-throughput OLAP store, higher memory footprint
# - timescale:  PostgreSQL extension, reuses DATABASE_URL by default,
#               much lower memory footprint
# Defaults to timescale; set to clickhouse when using the ClickHouse setup.
#TIMESERIES_BACKEND=timescale

# --- ClickHouse (used when TIMESERIES_BACKEND=clickhouse) ---
#CLICKHOUSE_URL=http://localhost:8123
#CLICKHOUSE_USER=default
#CLICKHOUSE_PASSWORD=

# --- TimescaleDB (used when TIMESERIES_BACKEND=timescale) ---
# Optional: override the postgres connection used for time-series data.
# If unset, the timescale driver reuses DATABASE_URL (must point at a
# Postgres instance with the timescaledb extension available).
# TIMESCALE_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/uptimekit

# ============================================
# Redis (required for caching, rate limiting, and health checks)
# ============================================
REDIS_URL=redis://localhost:6379

# ============================================
# Port Configuration (optional)
# ============================================
DASH_PORT=3000

# ============================================
# Status Page Configuration
# ============================================
# Domain where status pages are accessible via slug (e.g., status.uptimekit.com/acme)
# Point this domain and any customer custom domains at the same port as APP_URL.
APP_STATUS_PAGE_DOMAIN=status.uptimekit.com
# NEXT_PUBLIC_STATUS_PAGE_DOMAIN=status.uptimekit.com

# ============================================
# Subscriber Notifications
# ============================================
# Set to "resend" or "smtp" to enable subscriber email delivery
EMAIL_PROVIDER=resend
# Sender used for subscriber notification emails
EMAIL_FROM="UptimeKit <status@example.com>"
# Required when EMAIL_PROVIDER=resend
RESEND_API_KEY=
# Required when EMAIL_PROVIDER=smtp
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
# Optional, defaults to true for port 465 and false otherwise
SMTP_SECURE=false

# ============================================
# Worker Configuration
# ============================================
# API key for worker authentication (generate a secure random string)
WORKER_API_KEY=change_me_to_a_secure_worker_api_key
WORKER_MAX_CONCURRENCY=20

# ============================================
# Apprise (optional, enables the Apprise integration)
# ============================================
# Base URL of an apprise-api server. Set automatically in docker-compose.
# Leave unset to disable the Apprise integration handler (it becomes a no-op).
# APPRISE_URL=http://localhost:8000
