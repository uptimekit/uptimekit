import type { ThemeManifest } from "../types";

export const manifest: ThemeManifest = {
    id: "spark",
    name: "Spark",
    description: "Theme inspired by incident.io",
    version: "1.0.0",
    supportsDarkMode: true,
};
